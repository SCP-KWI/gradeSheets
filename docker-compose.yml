version: "3.9"
services:
  gradesheets:
    image: nginx:alpine
    container_name: gradesheets
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    networks:
      - nginxproxymanager_default
networks:
  nginxproxymanager_default:
    external: true
